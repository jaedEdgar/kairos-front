const baseUrl = "http://localhost:8000/api";
const urls = {
  Notebooks: `${baseUrl}/notebooks`,
  Notebook: `${baseUrl}/notebook`,
  Tasks: `${baseUrl}/tasks`,
  Task: `${baseUrl}/task`,
};

export default urls;
